{
  "name": "demo-idyll",
  "version": "1.0.0",
  "license": "MIT",
  "scripts": {
    "start": "idyll index.idyll --theme github --css styles.css --watch",
    "build": "idyll index.idyll --theme github --css styles.css",
    "deploy": "npm run build && gh-pages -d ./build"
  },
  "dependencies": {
    "d3": "^4.0.0",
    "idyll": "^6.0.0-alpha.6",
    "idyll-d3-component": "^2.0.0",
    "react-simple-pie-chart": "^0.5.0"
  },
  "devDependencies": {
    "gh-pages": "^0.12.0"
  }
}
